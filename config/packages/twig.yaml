twig:
    file_name_pattern: '*.twig'
    globals:
        app_name: '%env(string:APP_NAME)%'
        firm_logo_path: '%firm_logo_public_path%'
        firm_user_types: '%firm.user_types%'

when@test:
    twig:
        strict_variables: true
